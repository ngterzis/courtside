import { Placeholder } from './_placeholder';

export default function ChatRoute() {
  return <Placeholder title="Ask the agent" note="AI chat — streams responses, context auto-injected server-side." />;
}
